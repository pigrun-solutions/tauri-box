export type Settings = {
    id?: number
    ip: string
    lat: number
    long: number
    port: number
    coilFreq: number
    geoFreq: number
    duration: number
}

export type SingleBox = {
    deviceType?: string
    uid: number
}
